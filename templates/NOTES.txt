{{- if .Values.wordpress.ingress.enabled }}
Tu aplicación WordPress ha sido desplegada. Puedes acceder a ella en la siguiente URL:
http://{{ .Values.wordpress.ingress.host }}
{{- else }}
Tu aplicación WordPress ha sido desplegada, pero no se ha creado un Ingress.
Para acceder a ella, puedes usar 'kubectl port-forward':
  kubectl port-forward svc/{{ include "practica-final-k8s.fullname" . }} 8080:80
Y luego abrir http://localhost:8080 en tu navegador.
{{- end }}

Para obtener el estado de tu despliegue, ejecuta:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}